export interface Proveedor{

    // idProveedor:string,
    idProveedor:number,
    nombreCompleto: string,
    cedula:string,
    direccion:string, 
    telefono:string, 
    correo :string
}

// Recordar los nombres con los inciales obtienes los datos
// pero no podras insertar, lo otros es al revez es por vos que sos, modifica el
// backend